# Copyright 2026 NWChemEx-Project
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

[build-system]
requires = [
    "scikit-build-core>=0.10",
    "setuptools-scm>=8",
    # gau2grid (fetched transitively via GauXC) invokes a Python codegen
    # script at CMake configure/build time that imports numpy -- needed in
    # the isolated build env even though scikit-build-core's build-system
    # deps are otherwise C++-only. Capped below 2: newer numpy releases only
    # ship manylinux2014 wheels for a GCC >= 10.3 build, and manylinux2014's
    # own GCC 10.2.1 can't build numpy from source either, so an unpinned
    # requirement falls back to a source build that fails outright. This
    # cap only affects the isolated build env, not the runtime numpy version
    # a user ends up with (see requirements.txt for that).
    "numpy<2",
]
build-backend = "scikit_build_core.build"

[project]
name = "nwchemex-scf"
dynamic = ["version"]
description = "Self-consistent field (SCF) methods for the NWChemEx ecosystem"
license = { text = "Apache-2.0" }
requires-python = ">=3.8"
dependencies = [
    "pybind11",
    "nwchemex-nwxcmake>=0.1.0",
    "nwchemex-simde>=0.0.77",
]

[project.optional-dependencies]
# friendzone is needed by build/_deps/nwchemex-src/python/nwchemex/load_modules.py
# (see tests/python/conftest.py) -- unlike nwchemex itself, friendzone has no
# C++/CMake counterpart for scf to fetch, and (unlike nwchemex) it doesn't
# depend on scf, so it's safe to pull in normally here.
dev = ["pytest", "pre-commit", "nwchemex-friendzone[molssi]>=1.0.23"]

[tool.scikit-build]
metadata.version.provider = "scikit_build_core.metadata.setuptools_scm"
# All installed content comes from the CMake install; no pure-Python packages.
wheel.packages = []
build-dir = "build"

[tool.setuptools_scm]
fallback_version = "0.0.0"

# For editable (developer) installs, also build the tests.
[[tool.scikit-build.overrides]]
if.state = "editable"
cmake.args = ["-DDEVELOPER_SETUP=ON"]
